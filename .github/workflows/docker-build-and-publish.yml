name: Build & Publish Docker Image

on:
  workflow_dispatch:
    # branches: [ "main" ]
    inputs:
      version:
        type: string
        required: true


jobs:
  build:
    runs-on: self-hosted
    steps:
    - uses: bossanova456/gh-actions
    - name: Build and Publish Docker Image
      with:
        app-name: chandler-cup-backend
        version: ${{inputs.version}}