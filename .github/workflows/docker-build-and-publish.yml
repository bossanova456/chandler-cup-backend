name: Build & Publish Docker Image

on:
  workflow_dispatch:
    # branches: [ "main" ]
    inputs:
      version:
        type: string
        required: true


jobs:
  build-and-publish:
    runs-on: self-hosted
    steps:
    - name: Build and Publish Docker Image
      uses: bossanova456/gh-actions@v0.0.2
      with:
        app-name: chandler-cup-backend
        version: ${{inputs.version}}